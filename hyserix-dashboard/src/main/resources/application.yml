server:
  port: 1100

spring:
  application:
    name: Hystrix-Dashboard

eureka:
  instance:
    hostname: client-hystrix-dashboard
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/,http://localhost:8090/eureka/

hystrix:
  dashboard:
    proxy-stream-allow-list: "*"